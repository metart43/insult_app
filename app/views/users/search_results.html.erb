<div class="all_results col-8">
  <div class="show_heading">
    <span><b>Search Results For: <%= @query %></b></span>
  </div>
  <div class="search-box">
    <% @users.each do |u| %>
      <p><%= link_to "@#{u.username}", user_path(u) %></p>
    <% end %>

    <% if @users.empty? %>
      <h3>Sorry! No results found</h3>
    <% end %>
  </div>
</div>
